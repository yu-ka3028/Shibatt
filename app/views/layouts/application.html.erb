<!DOCTYPE html>
<html>
  <head>
    <title>Shibatt</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>

  <!-- Google tag (gtag.js) -->
  <script async src="https://www.googletagmanager.com/gtag/js?id=G-X2BLLMLSBJ"></script>
  <script>
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());

    gtag('config', 'G-X2BLLMLSBJ');
  </script>

  <!-- LINE LIFF SDK -->
  <script src="https://static.line-scdn.net/liff/edge/2/sdk.js"></script>

  </head>

  <body>
    <script>
      // ユーザーの設定に基づいてテーマを切り替える
      const darkModeMediaQuery = window.matchMedia('(prefers-color-scheme: dark)');
      const bodyElement = document.querySelector('body');

      function switchTheme(e) {
        const theme = e.matches ? 'coffee' : 'lemonade';
        bodyElement.setAttribute('data-theme', theme);
      }

      // 初期設定
      switchTheme(darkModeMediaQuery);

      // 設定が変更されたときにテーマを切り替える
      darkModeMediaQuery.addListener(switchTheme);
    </script>

    <% if logged_in? %>
      <%= render 'shared/header' %>
    <% else %>
      <%= render 'shared/before_login_header' %>
    <% end %>
    <%= render 'shared/flash_message' %>
    
    <%= yield %>
    
    <%= render 'shared/footer' %>
    <%= javascript_include_tag 'liff_set' %>
  </body>
</html>
